<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style1.css">
    <script src="https://kit.fontawesome.com/6a2d5f8336.js" crossorigin="anonymous"></script>
    <title>Document</title>
</head>

<body class="bg-lightgrey">
    <%- include ("navbar") %>
        <%- include ("sidebar") %>
            <form method="POST" action="/updateCar/<%= initialData.id %>" class="main" enctype='multipart/form-data'>
                <%- include ("top", {form: "update" , home: false}) %>
                    <!--content-->
                    <div class=" bg-white form-size py-3 px-5">
                        <div class="form-list d-flex margin-bot-20">
                            <label class="labelnama fs-6 text-secondary">Nama<sup class="color-red">*</sup></label>
                            <input type="text" class="input-size" name="nama" placeholder="Avanza"
                                value="<%= initialData.model %>">
                        </div>
                        <div class="form-list d-flex margin-bot-20">
                            <label class="labelnama fs-6 text-secondary">Sewa Per Hari<sup
                                    class="color-red">*</sup></label>
                            <input type="text" class="input-size" name="sewa" placeholder="rp. 0,-"
                                value="<%= initialData.rentPerDay %>">
                        </div>
                        <div class="form-list d-flex flex-row margin-bot-20">
                            <label class="labelnama fs-6 text-secondary">Ukuran<sup class="color-red">*</sup></label>
                         
                            <select class="form-select input-size" aria-label="Default select example" name="ukuran"
                                value="<%= initialData.carSize %>">
                                <% for(let j = 0; j < availableSize.length; j++){ %>
                                    <% if (availableSize[j].id === initialData.carSize){ %>
                                        <option value="<%= availableSize[j].name %>" selected><%= availableSize[j].name %></option>
                                    <% }else { %>
                                        <option value="<%= availableSize[j].name %>"><%= availableSize[j].name %></option>
                                    <% } %>
                                    
                                <% } %>
                                
                            </select>
                        </div>
                        <div class="form-list d-flex">
                            <label class="labelnama fs-6 text-secondary">Foto<sup class="color-red">*</sup></label>
                            <input type="file" class=" input-size d-none" name="foto" id="file-input">
                            <input type="text" class=" input-size input-cursor" name="fotos"
                                placeholder="https://placeimg.com/640/480/any" id="input-file" readOnly
                                value="<%= initialData.image %>">
                        </div>
                        <div>
                            <p class="foto-text">
                                File size max. 2MB
                            </p>
                        </div>
                    </div>
                    <div class="d-flex justify-content-start bottom-left">
                        <a type="button" class="btn btn-outline-primary darkblue04 button-size bg-white"
                            href="/home">cancel</a>
                        <button type="submit" class="btn btn-primary bg-darkblue04 button-size ms-3">save</button>
                    </div>
                    <!--content-->

            </div>
            <!-- JavaScript Bundle with Popper -->
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
                integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
                crossorigin="anonymous"></script>
</body>
<script src="https://unpkg.com/axios/dist/axios.min.js"></script>
<script src="/scripts/form.js"></script>
</html>